<UserControl x:Class="WindowsAuditTool.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="{StaticResource BgBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0"
                Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button x:Name="OpenBrowserButton"
                            Content="Open in Browser"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0"
                            Click="OpenBrowserButton_Click"/>
                    <Button x:Name="OpenFolderButton"
                            Content="Open Folder"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0"
                            Click="OpenFolderButton_Click"/>
                    <TextBlock x:Name="PdfStatus"
                               FontSize="12"
                               Foreground="{StaticResource MutedBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <Button x:Name="RunAgainButton"
                            Content="Run Again"
                            Style="{StaticResource PrimaryButton}"
                            Click="RunAgainButton_Click"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Fill="{StaticResource GoodBrush}" Margin="0,0,8,0"/>
                    <TextBlock x:Name="StatusLabel"
                               Text="Audit Complete"
                               FontSize="15"
                               FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- WebView2 report viewer -->
        <wv2:WebView2 x:Name="ReportWebView"
                       Grid.Row="1"
                       Visibility="Visible"/>

        <!-- Fallback message (shown when WebView2 is unavailable) -->
        <Border x:Name="FallbackPanel"
                Grid.Row="1"
                Visibility="Collapsed"
                Background="{StaticResource CardBrush}"
                CornerRadius="14"
                Padding="40"
                Margin="40"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="Report Generated"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,12"/>
                <TextBlock x:Name="FallbackPath"
                           FontSize="13"
                           Foreground="{StaticResource MutedBrush}"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"/>
                <TextBlock Text="WebView2 runtime not available. The report has been opened in your default browser."
                           FontSize="12"
                           Foreground="{StaticResource MutedBrush}"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
