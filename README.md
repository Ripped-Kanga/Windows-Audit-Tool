# Windows-Audit-Tool
The Windows Audit Tool is a small lightweight powershell script wrapped as and executable. It produces a markdown report that can be later converted to another format (currently docx is the target conversion format). 
